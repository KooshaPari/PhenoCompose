# Testing Strategy

- Validate package.json and .releaserc.json parse as JSON.
- Validate .github/workflows/release.yml renders correctly in the diff and follows existing action pinning conventions.
- Run a focused git diff review after edits.
