# Session Overview

- Goal: add a repo-level \`pre-commit\` configuration covering \`prettier\`, \`eslint\`, \`tsc\`, \`vitest\`, \`markdownlint\`, and \`gitleaks\`.
- Scope: root JS/tooling config, \`package.json\`, and this session record.
- Success criteria: the repo has installable scripts/config for each requested hook and a valid \`.pre-commit-config.yaml\` wiring them together.
