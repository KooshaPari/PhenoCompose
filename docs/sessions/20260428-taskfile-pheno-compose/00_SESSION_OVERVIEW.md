# Session Overview

- Goal: replace the stale Rust-only Taskfile with repo-aware tasks for build, test, lint, and clean.
- Scope: `Taskfile.yml` at the repo root and the session notes under `docs/sessions/`.
- Success criteria: the Taskfile reflects the repo's Go root, Rust driver, and docs/playwright manifests.
