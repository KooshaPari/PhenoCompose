# Known Issues

- This worktree currently has no \`node_modules/\`, so live startup verification depends on a later install step.
- The preload intentionally stays inactive until an OTLP endpoint env var is provided.
