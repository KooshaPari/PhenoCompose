# Known Issues

- The repository currently has no tests directory, so coverage value remains dependent on future Vitest suites.
- No lockfile is committed today, so dependency resolution is not pinned by this change.
