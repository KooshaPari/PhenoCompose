# pheno-config

Typed configuration primitives for PhenoCompose documentation surfaces.

This package currently exposes the shared docs site configuration consumed by
VitePress and is structured so other documentation repos can import the same
typed data without copying inline config blocks.
